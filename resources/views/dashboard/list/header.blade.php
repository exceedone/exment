<div>
    <div class="btn-group pull-right">
        @if(isset($new_url))
        <a href="{{$new_url}}" class="btn btn-sm btn-success">
            <i class="fa fa-save"></i>
        </a>
        @endif
    </div>
</div>